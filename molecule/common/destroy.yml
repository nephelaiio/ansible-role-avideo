---
- name: destroy

  hosts: localhost

  connection: local

  gather_facts: false

  tasks:

    - name: remove docker container
      community.docker.docker_container:
        name: "{{ inventory_hostname }}"
        state: absent
        force_kill: "{{ force_kill | default(True) }}"
